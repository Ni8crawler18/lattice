.env
env/
__pycache__/
*.pyc
.vercel
node_modules/
.next/
server/raw_cache.json
server/api_keys.json
server/geo_cache.json

# Google OAuth client secret — never commit
client_secret_*.json
.env.local

# local traction ledger fallback — holds real emails; production uses Postgres
server/users.json

# runtime parse cache — keyed on raw address text, so it accumulates whatever
# anyone types. Never commit it; parsed_cache.json (the seed-set cache eval.py
# needs) is the only address cache that belongs in the repo.
server/parse_cache.json

# local test assets — not part of the product
data/IFSC.csv
data/test-audio.mp3
test/

fastapi>=0.115
uvicorn[standard]>=0.32
sarvamai>=0.1.28
python-dotenv>=1.0
# Pinned below 2.0: mcp 2.x removed mcp.server.fastmcp, which lattice_mcp.py
# imports, so an unpinned >=1.10 resolved to 2.0.0 on a clean build and took
# the whole API down at import time (app.py mounts the MCP app at startup).
mcp>=1.10,<2
httpx>=0.27
psycopg2-binary>=2.9
